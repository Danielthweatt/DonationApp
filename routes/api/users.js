//Dependencies
const router = require('express').Router();
const usersController = require('../../controllers/usersController');

//User Routes (to be written...)
//router.route('').get(usersController.something);

module.exports = router;
